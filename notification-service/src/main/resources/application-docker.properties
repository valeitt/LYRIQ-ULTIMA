# =============================================
# NOTIFICATION SERVICE — Perfil DOCKER
# Activo con: SPRING_PROFILES_ACTIVE=docker (docker-compose)
# =============================================

spring.datasource.url=jdbc:mysql://mysql:3306/db_notification?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root
